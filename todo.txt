create a prompt set
simulate these prompts via llm (gpt40 via openrouter)
store them - postgres maybe
stuff to extract: brand mentions, cited urls, compititor mentions and response timestamp

get more clarity on - Provide a mechanism to generate or manage prompt variants.

expose all the data via api (fastapi, create proper endpoints to expose relevant information)
dashboard to show historical visibility/tracking information [yet to decide what kinda info to show, how to show tracking exactly]

get more clarity on - handle failures such as timeout, rate limit, malformed model output, and provider/API errors

document how to normalize results => proposed solution is to enforce a proper schema